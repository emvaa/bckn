# Configuración del Servidor
PORT=3000
HOST=0.0.0.0

# Configuración de CORS
# Si CORS_ALLOW_ALL=true, permite acceso desde cualquier origen (útil para desarrollo)
# Si CORS_ALLOW_ALL=false, usa la lista blanca de orígenes
CORS_ALLOW_ALL=true

# URL del Frontend (opcional, solo necesario si CORS_ALLOW_ALL=false)
FRONTEND_URL=http://localhost:8080

# Configuración de Base de Datos
DATABASE_URL="postgresql://usuario:password@localhost:5432/nombre_db"

# JWT Secret (cambiar en producción)
JWT_SECRET=tu_secret_key_muy_segura_aqui

# Entorno
NODE_ENV=development
